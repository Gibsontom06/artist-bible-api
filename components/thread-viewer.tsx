export function ThreadViewer() {
  return (
    <section className="rounded border border-zinc-800 p-3">
      <h2 className="font-semibold">Thread Viewer</h2>
      <p className="text-sm text-zinc-400">Displays Gmail messages + attachments per thread.</p>
    </section>
  );
}
